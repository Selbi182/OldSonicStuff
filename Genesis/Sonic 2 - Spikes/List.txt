Top priority:
- Build first real levels.
- Add black spikes to replace blue spikes and make those neutral.

Medium priority:
- Better blood.

Low priority:
- Add an undoing feature.
- Smoothing camera movement when touching unevenly placed spike.
- Fix slowdown on level startup caused by NemDec.
- Change in-game-placed spikes to use a custom respawn table rather than being simply not deleted.
- Add clone driver V2.